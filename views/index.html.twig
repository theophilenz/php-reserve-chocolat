{% extends "layout.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block contenu %}
{% for chocolat in chocolats %}
<article>
  <h2><a href="{{ path ('chocolat', {'id':chocolat.id}) }}">{{chocolat.titre}}</a></h2>
  <p>{{chocolat.description}}</p>
</article>
{% endfor %}
{% endblock %}